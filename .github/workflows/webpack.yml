name: NodeJS with Webpack

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
https://github.com/JwhoknowsWhocaresP/JwhoknowsWhocaresP/tree/main
jobs:punished to full extent app was left to me till okta/passkeyed i give zero consent till paid back 
  build:cease use no permisson till what stolen has been returned to everyone,all files programes and codeing remain 100% mine
  
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}

    - name: Build
      run: |
        npm install
        npx webpack
